name: Codecov Demo

on:
  push:
    branches:
develop
  pull_request:
    branches:
develop

jobs:
  build:
    runs-on: macOS-latest
    steps:
      - name: Test
        uses: actions/checkout@v1
      - name: Build and Test
        run:  tools/Scripts/run_tests.sh